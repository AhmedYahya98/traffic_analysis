# Model Configuration
model:
  type: "yolo11"                    # Options: yolov8, yolov5, roboflow
  weights_path: "yolo11n.pt" # Path to model weights
  confidence: 0.3                   # Detection confidence threshold
  iou: 0.7                          # IOU threshold for NMS
  device: "cuda"                    # Options: cuda, cpu, mps (Mac)

# Input Configuration
input:
  source_type: "video"              
  video_path: "data/input/videos/input.mp4" # Path to input video file
  webcam_id: 0                      
  image_folder: "data/input/images/" 

# Tracking Configuration
tracking:
  enabled: true
  track_thresh: 0.25                # Tracking confidence threshold
  track_buffer: 30                  # Frames to keep lost tracks
  match_thresh: 0.8                 # IOU threshold for matching

# Analysis Configuration
analysis:
  # Vehicle classes to detect (from data/dataset2/data.yaml)
  # Dataset2 classes: 0=PMT, 1=articulated-bus, 2=bus, 3=car, 4=freight, 5=motorbike, 6=small bus, 7=truck
  target_classes: [1, 2, 3, 5, 7]      # articulated-bus, bus, car, motorbike, truck
  class_names:
    0: "PMT"
    1: "articulated-bus"
    2: "bus"
    3: "car"
    4: "freight"
    5: "motorbike"
    6: "small-bus"
    7: "truck"
  
  # Zone configuration (define counting zones)
  zones:
    - name: "entry_zone_1"
      polygon: [[783, 134], [833, 134], [833, 254], [783, 254]] # [top-left, top-right, bottom-right, bottom-left]
    - name: "exit_zone_1"
      polygon: [[440, 92], [527, 92], [527, 162], [440, 162]]
    - name: "entry_zone_2"
      polygon: [[447, 346], [520, 346], [520, 487], [447, 487]] # [top-left, top-right, bottom-right, bottom-left]
    - name: "exit_zone_2"
      polygon: [[717, 466], [827, 466], [827, 547], [717, 547]] # [top-left, top-right, bottom-right, bottom-left]


# Visualization Configuration
visualization:
  show_boxes: true                  # Draw bounding boxes
  show_labels: true                 # Show class labels
  show_tracks: true                 # Show tracking IDs
  show_zones: true                  # Draw counting zones
  show_trails: true                # Show vehicle trails
  trail_length: 30                  # Trail history length
  thickness: 2                      # Line thickness
  font_scale: 0.6                   # Text size

# Output Configuration
output:
  save_video: true
  video_path: "data/output/videos/annotated_traffic.mp4"
  save_json: true
  json_path: "data/output/json/detections.json"
  save_csv: true
  csv_path: "data/output/csv/statistics.csv"
  fps: 30                           # Output video FPS